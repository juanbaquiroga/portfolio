# Firebase Client SDK Configuration
# These are used for client-side Firebase services (Auth, Firestore, etc.)
# You can find these in your Firebase project settings under "General".
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-auth-domain"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-storage-bucket"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="your-measurement-id"

# Admin User Configuration
# This email address will be granted admin privileges.
NEXT_PUBLIC_ADMIN_EMAIL="your-admin-email@example.com"

# Vercel Blob Storage
# This token is required for uploading files to Vercel Blob.
# You can create this token in your Vercel project settings.
BLOB_READ_WRITE_TOKEN="your-vercel-blob-read-write-token"

# Firebase Admin SDK Configuration
# These are used for server-side services (e.g., session management in middleware).
# You need to create a service account in your Firebase project and provide the details here.
# It is recommended to JSON.stringify the service account key and paste it here.
FIREBASE_SERVICE_ACCOUNT_KEY='your-firebase-service-account-key-json'